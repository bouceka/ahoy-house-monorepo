// @flow
import * as React from 'react';

interface ImageData {
  id: string;
  imgUrl: string;
  alt: string;
}

const IMAGE_DATA: ImageData[] = [
  {
    id: '1',
    imgUrl: '',
    alt: '',
  },
];

type Props = {};
export const PropertyImageCarousel = (props: Props) => {
  return <div></div>;
};
